# PI → Agent Directives — 2026-05-25 11:00 UTC

> **Workflow change**: From now on PI ↔ Agent communication is **file-based via git push/pull**.
> - PI writes directives to `analysis/COMM_PI_TO_AGENT/<timestamp>_<topic>.md`
> - Agent replies in `analysis/COMM_GPU5_<date>.md` (or `COMM_AGENT_TO_PI_<timestamp>.md` for new threads)
> - Push cadence: agent every 4 hours per `SESSION_NOTES.md`; PI replies on read.

## 1. Acknowledged: current run plan looks correct

PI confirms the plan in `SESSION_NOTES.md`:
- exp_v1_orchestrator on GPU 0–2 (3 of 7 methods training, queue of 4)
- p0_reeval_orchestrator on GPU 3–7 (5 done, 5 running, 57 queued)
- Push cadence: every 4 hours, format `"P0: N/67 done; exp_v1: M/7 trained, K/7 evaluated"`

**No need to ask permission to continue.** Execute autonomously.

## 2. Priorities for the 11:37 UTC push (1st 4-hr checkpoint)

Hard requirements before pushing:

| # | Item | Status target by 11:37 |
|---|---|---|
| A | Mistral-7B PI cells (8 cells) re-evaluated | **≥ 5 done** with OLD-vs-NEW table |
| B | exp_v1 first 3 cells (vanilla / baseline / random_drop) | **trained ≥ 1, eval triggered ≥ 1** |
| C | Documented: any unexpected GPU failure / OOM / hang | log path + 1-line root cause |

If Item A misses (< 5 mistral PI done), root-cause in commit message — don't pad with non-PI cells.

## 3. exp_v1 eval trigger — DO NOT WAIT for all 7 to train

`SESSION_NOTES.md` line 63-69 flagged this as critical missing piece. PI's instruction:

> **The moment a cell's `summary.json` appears, fire `scripts/exp_v1_eval.py` against it on any free GPU. Do not wait for all 7.**

Suggested logic (you decide implementation):
- Either extend `exp_v1_orchestrator.py` with a post-train `exp_v1_eval.py` hook
- Or run `exp_v1_eval.py --watch` as a separate background process from launch

The exp_v1 sweep is the **primary scientific result** of this push cycle. Train alone is half the work.

## 4. OLD vs NEW table format (for P0 re-eval)

When committing P0 re-eval results, include a table like:

```
| cell                                              | OLD strict | NEW strict | Δ      | OLD flex | NEW flex | Δ      |
|---------------------------------------------------|-----------:|-----------:|-------:|---------:|---------:|-------:|
| mistral-7b/metamathqa/relora_diag_gated_S3pos     | 36.47      | XX.XX      | ±X.XX  | 36.39    | XX.XX    | ±X.XX  |
| mistral-7b/metamathqa/relora_random_drop          | 36.47      | XX.XX      | ±X.XX  | 36.39    | XX.XX    | ±X.XX  |
```

If NEW S3pos and NEW random_drop are still bit-identical post-fix, that's a critical finding — flag it in commit message **immediately**, do not ignore.

## 5. Things PI does NOT need this cycle

- Multi-seed (commit D) can wait until single-seed sweep (commit C) is done and eyeballed
- Other-model eval (qwen3-1.7B, qwen35-* sweep) waits for qwen3-8b sweep conclusion
- Forgetting / OOD eval (MMLU, BBH) deferred — first finish core sweep

## 6. Reply mechanism

If PI direction is unclear or you hit a hard blocker:
- Create `analysis/COMM_AGENT_TO_PI_<timestamp>.md` with **specific question + 2-3 options for PI to choose**
- Don't stall: continue executing the unblocked parts in parallel
- PI checks repo at agent's push cadence + ad-hoc

## 7. Hard rules (unchanged)

- All evals via vLLM-on-merged. No HF backend.
- Save full evidence: dropped_components.jsonl, saliency_at_merge.jsonl, effective_rank.jsonl, condition_number.jsonl, train_log.jsonl, lm_eval/.
- Never delete pre-fix lm_eval — rename to `lm_eval_PRE_P0_FIX_TAINTED/`.
- Push every 4 hours regardless of completion state. Better partial than silent.

---

**Acknowledgment**: agent should write `ACK: PI directives 2026-05-25 11:00` in the next push commit message body to confirm read.
