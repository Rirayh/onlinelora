# Context Snapshot 2026-05-18 15:10 — Phase A+B done, Phase C smokes running

## STATUS
- ✅ All 5 new model downloads complete (R1-Distill, AceReason, OLMo-2, Gemma-3-12B, Llama-3-8B already local)
- ✅ COLA implemented in `scripts/stage3_run.py` (METHOD_CHOICES + dispatcher: do_relora=True, gate_sign=None — equivalent to relora_baseline; existing merge code already does kaiming A + zero B + fresh AdamW)
- ✅ `--attn_implementation` flag added (default sdpa, eager for Gemma-3)
- ✅ Phase C scout queue script written: `scripts/run_phase_c_scout.sh` (50 cells = 5 models × 2 datasets × 5 methods, auto-fills GPUs with >50GB free)

## Active smokes (5 GPUs)
- GPU 3 (PID 1460464): qwen3-8b/tulu3-sft/cola smoke (100 steps, ~step 50 done, MERGE EVENT 1 verified drop_rate=0.000 = correct COLA)
- GPU 5 (PID 1461584): olmo2-7b/mm/lora_vanilla smoke ✅ DONE (val_loss=0.2354)
- GPU 0 (PID 1462596): gemma3-12b/mm smoke (eager attn, slow — step 0 only after 5min)
- GPU 2 (PID 1462598): acereason-7b/mm smoke (step 0 only after 5min)
- GPU 4 (PID 1462600): llama3-8b/mm smoke (step 0 only after 5min)
  → SLOW reason: GPU 0/2/4 also running lm-eval batches (qwen25/mistral). Two heavy jobs on one GPU = 2x slowdown is acceptable.

## Lm-eval batch4 still running (GPU 0,1,2,4,5,6,7 all have lm_eval procs)
- GPU 0,1,2,4: qwen25/mm (vanilla/baseline/S3pos/dora) step_1500/750
- GPU 6,7: mistral/mm + qwen3/mm dora (mistral procs write to wrong path, will fix after)

## NEW PEAK from batch3 (commit e0621e1)
- qwen3/tulu/S3neg step_003000: GSM8K 88.32% (top of 27-row multickpt table)

## Models locations
| key | path |
|---|---|
| gemma3-12b | /mnt/cpfs/junlongke/onlinelora/models/gemma-3-12b-it |
| llama3-8b  | /mnt/cpfs/public_data/public_model/Meta-Llama-3-8B |
| r1-distill-7b | /mnt/cpfs/junlongke/onlinelora/models/R1-Distill-Qwen-7B |
| acereason-7b | /mnt/cpfs/junlongke/onlinelora/models/AceReason-Nemotron-7B |
| olmo2-7b | /mnt/cpfs/junlongke/onlinelora/models/OLMo-2-7B |

## Next steps
1. Wait for current 4 smokes (gemma3/acereason/llama3 in progress; cola almost done) to complete or hit step ≥ 25 to verify training is healthy
2. Once verified, kill smokes (smoke uses `seed42_smoke` dirs, harmless), then launch `bash scripts/run_phase_c_scout.sh` in nohup
3. Phase C ETA: ~50 cells × ~2-3h each / 8 parallel = ~12-18h
4. After Phase C: cherry-pick top-3 (model, dataset) where Δ_S3pos ≥ +1.5pp, then Phase D with remaining 4 methods (S3neg, train_gated, adalora, cola)

## Key bug fixes applied
- `dirname x2 + ..` → `dirname x2` for output dir resolution in launch scripts
- Qwen2.5 base path is `/Qwen/Qwen2.5-7B` not `/Qwen2.5/Qwen2.5-7B`
- Mistral lm_eval_step* dirs already moved into `seed42/`

## Commands to remember
```bash
# refresh tables
/mnt/cpfs/junlongke/miniconda3/envs/espo/bin/python scripts/build_main_table.py
/mnt/cpfs/junlongke/miniconda3/envs/espo/bin/python scripts/build_multickpt_table.py

# launch phase C scout
nohup bash scripts/run_phase_c_scout.sh > logs/scout/_runner.log 2>&1 &

# check ckpt after smoke pass
ls results/stage3_v2/*/seed42_smoke
```

## Smoke results (15:13)
- ✅ COLA smoke (qwen3/tulu): MERGE 1 at step 50 OK, drop_rate=0.000, val_loss 1.39→1.39 (correct math)
- ✅ OLMo-2-7B smoke: val_loss=0.2354, training works
- ✅ AceReason-7B: step 25/50 train_loss=0.7485 (training healthy, ~9s/step)
- ✅ Llama-3-8B: step 25/50 train_loss=0.5569 (training healthy, ~11s/step)
- ⏳ Gemma-3-12B: still at step 0 after 7min — eager attention very slow on 12B; may be 2-3x slower than 7B. Running but expensive.

ALL 6 SMOKES PASS BASIC SANITY (5 model loads + 1 COLA dispatcher).

## Remaining issues / TODO
- Gemma-3-12B is bottleneck: confirm if a single cell will fit in scout time budget, may need to skip in Phase C or run with --batch_size 2
- Mistral lm-eval procs (PID 1449566/1449571) still in flight to wrong path; move after they finish

## Commit message to use when committing
"Phase A+B: COLA method + new model lineup smokes pass

- Add cola to METHOD_CHOICES (functionally relora_baseline with K-stage hyperparam intent)
- Add --attn_implementation argparse (sdpa/eager/flash_attention_2)
- Phase C scout queue script: run_phase_c_scout.sh (auto-fills GPUs >50GB free)
- Smoke verified: cola dispatcher + OLMo-2/AceReason/Llama-3-8B/Gemma-3-12B model loading"

## CONTEXT-CLEAR HANDOFF (15:14)

### Where we are RIGHT NOW
Just committed `f455e06` (Phase A+B), GitHub PUSH REJECTED due to HF token in
`10_cloud_agent_prompt_model_pivot_cola.md:32` and copy at `scripts/10_cloud_agent_prompt_model_pivot_cola.md:32`.

Token to redact: `hf_REDACTED`
(literally that exact string in 2 file locations)

### Immediate next steps after context clear
1. Redact token in BOTH files (replace with `hf_REDACTED`):
   ```
   sed -i 's/hf_REDACTED/hf_REDACTED/g' \
     /mnt/cpfs/junlongke/onlinelora/lora_obd/10_cloud_agent_prompt_model_pivot_cola.md \
     /mnt/cpfs/junlongke/onlinelora/lora_obd/scripts/10_cloud_agent_prompt_model_pivot_cola.md
   ```
2. `git add -A && git commit --amend --no-edit && git push origin main`
3. Wait for current 5 smokes to finish (gemma3 still slow, others should be done):
   - PIDs ~ 1460464 (cola), 1462596 (gemma3), 1462598 (acereason), 1462600 (llama3)
4. Once smokes done + verified healthy:
   ```
   nohup bash scripts/run_phase_c_scout.sh > logs/scout/_runner.log 2>&1 &
   ```
5. Check status periodically; refresh tables on incoming results.

### Current GPU layout (estimate)
- GPU 0: lm_eval (qwen25/mm/vanilla step_1500) + gemma3 smoke
- GPU 1: lm_eval qwen25/mm/baseline step_1500
- GPU 2: lm_eval qwen25/mm/S3pos + acereason smoke (TIGHT MEM!)
- GPU 3: cola smoke (almost done)
- GPU 4: lm_eval qwen25/mm/dora + llama3 smoke
- GPU 5: free now (olmo smoke done)
- GPU 6: lm_eval mistral/mm/S3pos
- GPU 7: lm_eval qwen3/mm/dora + mistral/mm/dora

### Phase C scout queue script behavior
- 50 cells = 5 models × 2 datasets × 5 methods (vanilla, baseline, S3pos, random_drop, dora)
- Picks any GPU with >50GB free
- Skips cells with existing summary.json
- Runs 3000 steps (800 for dora)
- Logs to logs/scout/${model}-${dataset}-${method}.log

### After Phase C: Phase D
Cherry-pick top-3 (model, dataset) by Δ_S3pos = GSM8K(S3pos) - max(others) ≥ +1.5pp
Then add S3neg, train_gated, adalora, cola for those 3 cells + multi-seed (1, 7).

### Key file paths
- `scripts/stage3_run.py` — main training (cola added, --attn_implementation added)
- `scripts/run_phase_c_scout.sh` — auto-fill 8 GPUs queue
- `scripts/build_main_table.py` — refresh main_table from best/lm_eval_v2
- `scripts/build_multickpt_table.py` — refresh multickpt_table from lm_eval_step*/
- `10_cloud_agent_prompt_model_pivot_cola.md` — full plan (root)
- `.context_snapshot.md` — THIS file, the live handoff

